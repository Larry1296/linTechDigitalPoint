import{render,screen}from"@testing-library/react";import{MemoryRouter}from"react-router-dom";import{beforeAll,describe,expect,it,vi}from"vitest";import{ThemePicker}from"./components/ThemePicker";import{LoginPage}from"./pages/auth/AuthPages";import{AuthProvider}from"./auth/AuthContext";
beforeAll(()=>vi.stubGlobal("fetch",vi.fn().mockResolvedValue({status:200,ok:true,json:async()=>({authenticated:false,is_staff:false,is_superuser:false,roles:[],permissions:[]})})));
describe("application experience",()=>{it("defaults theme selector to System",()=>{render(<ThemePicker/>);expect(screen.getByRole("combobox")).toHaveValue("system")});it("has a distinct staff login",()=>{render(<MemoryRouter><AuthProvider><LoginPage staff/></AuthProvider></MemoryRouter>);expect(screen.getByRole("heading",{name:"Staff login"})).toBeInTheDocument()})});
