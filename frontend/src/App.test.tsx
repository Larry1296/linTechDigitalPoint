import {render,screen} from "@testing-library/react"; import {MemoryRouter} from "react-router-dom"; import {describe,expect,it} from "vitest"; import App from "./App"; describe("application shell",()=>{it("defaults theme selector to System",()=>{render(<MemoryRouter><App/></MemoryRouter>);expect(screen.getByRole("combobox")).toHaveValue("system");expect(screen.getByText("Your trusted digital point.")).toBeInTheDocument()})});
