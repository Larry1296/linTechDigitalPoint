import{useEffect,useState}from"react";import{Link}from"react-router-dom";import{api}from"../../api/client";import{Empty,ErrorState,Loading}from"../../components/States";import type{Cart}from"../../types";export function CartPage(){const[cart,setCart]=useState<Cart>();const[error,setError]=useState("");const load=()=>api<Cart>("/api/v1/commerce/cart/").then(setCart).catch(e=>setError(e.message));useEffect(()=>{void load()},[]);if(error)return <main><ErrorState message={error}retry={load}/></main>;if(!cart)return <Loading/>;return <main><h1>Your cart</h1>{!cart.items.length?<Empty>Your cart is empty. <Link to="/shop">Continue shopping</Link></Empty>:<><div className="list">{cart.items.map(i=><article key={i.id}><div><b>{i.product_name}</b><small>{i.variant_name} · {i.sku}</small></div><input aria-label={"Quantity for "+i.product_name}type="number"min="0"step="1"value={i.quantity}onChange={async e=>setCart(await api("/api/v1/commerce/cart/items/"+i.id+"/",{method:"PATCH",body:JSON.stringify({quantity:e.target.value})}))}/><span>KSh {i.line_total}</span><button className="danger"onClick={async()=>setCart(await api("/api/v1/commerce/cart/items/"+i.id+"/",{method:"DELETE"}))}>Remove</button></article>)}</div><div className="cartTotal"><b>Total: KSh {cart.total}</b><Link className="button"to="/checkout">Proceed to checkout</Link></div></>}</main>}

