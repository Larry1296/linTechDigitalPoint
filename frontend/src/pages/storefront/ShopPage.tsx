import{useEffect,useState}from"react";import{Link}from"react-router-dom";import{api}from"../../api/client";import{Empty,ErrorState,Loading}from"../../components/States";import type{Product}from"../../types";export function ShopPage(){const[rows,setRows]=useState<Product[]>([]);const[query,setQuery]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");const load=()=>{setLoading(true);api<Product[]>("/api/v1/store/products/"+(query?"?search="+encodeURIComponent(query):"")).then(setRows).catch(e=>setError(e.message)).finally(()=>setLoading(false))};useEffect(load,[]);return <main><div className="heading"><div><span className="eyebrow">Unified live inventory</span><h1>Shop</h1></div><form onSubmit={e=>{e.preventDefault();load()}}><input aria-label="Search products"value={query}onChange={e=>setQuery(e.target.value)}placeholder="Search product, SKU or barcode"/><button>Search</button></form></div>{loading?<Loading/>:error?<ErrorState message={error}retry={load}/>:!rows.length?<Empty>No active products are available yet.</Empty>:<div className="productGrid">{rows.map(p=><article className="productCard"key={p.id}><div className="productImage">{p.images[0]?<img src={p.images[0].url}alt={p.images[0].alt}/>:p.product_name[0]}</div><small>{p.category}</small><h2>{p.product_name}</h2><p>{p.name} · {p.sku}</p><b>KSh {p.selling_price}</b><span className={p.available&&+p.available>0?"stock":"out"}>{p.available===null?"Service":+p.available!>0?p.available+" available":"Out of stock"}</span><Link className="button"to={"/products/"+p.id}>View product</Link></article>)}</div>}</main>}

