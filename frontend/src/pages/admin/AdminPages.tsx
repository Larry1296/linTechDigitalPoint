import{useEffect,useState}from"react";import{api}from"../../api/client";import{Empty,ErrorState,Loading}from"../../components/States";import type{Order,Product,Zone}from"../../types";
function useLoad<T>(url:string){const[data,setData]=useState<T>();const[error,setError]=useState("");const load=()=>api<T>(url).then(setData).catch(e=>setError(e.message));useEffect(()=>{void load()},[url]);return{data,error,load}}
export function DashboardPage(){const{data,error,load}=useLoad<any>("/api/v1/inventory/dashboard/");if(error)return <ErrorState message={error}retry={load}/>;if(!data)return <Loading/>;const metrics=[["Revenue",data.today.revenue],["COGS",data.today.cogs],["Gross profit",data.today.profit],["Sales",data.today.sales],["Stock cost",data.inventory.cost],["Retail value",data.inventory.retail],["Potential margin",data.inventory.potential_margin],["Low stock",data.inventory.low_stock]];return <><span className="eyebrow">Live business data</span><h1>Dashboard</h1>{data.inventory.total_skus===0&&<div className="callout"><div><b>Set up your physical shop</b><p>Teach LinTech where the existing shelves and stock are located.</p></div><a className="button"href="/admin-app/digital-shop">Configure Digital Shop</a></div>}<div className="metrics">{metrics.map(([label,value])=><article key={label}><span>{label}</span><strong>{typeof value==="number"&&label==="Sales"?value:"KSh "+value}</strong></article>)}</div></>}
type PosProduct={id:number;product_name:string;variant_name:string;sku:string;barcode:string;product_type:string;selling_price:string;available:string;locations:{zone:string;shelf_code:string;shelf_name:string;available:string}[]};
export function PosPage(){const[query,setQuery]=useState("");const[rows,setRows]=useState<PosProduct[]>([]);const[cart,setCart]=useState<{p:PosProduct;q:number}[]>([]);const[result,setResult]=useState<any>();const[error,setError]=useState("");const search=()=>api<PosProduct[]>("/api/v1/commerce/pos/catalog/?q="+encodeURIComponent(query)).then(setRows).catch(e=>setError(e.message));useEffect(()=>{void search()},[]);const add=(p:PosProduct)=>setCart(old=>old.some(x=>x.p.id===p.id)?old.map(x=>x.p.id===p.id?{...x,q:x.q+1}:x):[...old,{p,q:1}]);const complete=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const form=Object.fromEntries(new FormData(e.currentTarget));try{setResult(await api("/api/v1/commerce/pos/complete/",{method:"POST",body:JSON.stringify({...form,items:cart.map(x=>({variant_id:x.p.id,quantity:x.q})),idempotency_key:crypto.randomUUID()})}));setCart([])}catch(err){setError((err as Error).message)}};return <><span className="eyebrow">Counter selling</span><h1>Point of Sale</h1>{result&&<div className="callout receipt"><div><b>Sale complete · {result.receipt_number}</b><p>Total KSh {result.total}</p></div><button onClick={()=>window.print()}>Print receipt</button></div>}{error&&<p className="formError">{error}</p>}<div className="posGrid"><section><form className="search"onSubmit={e=>{e.preventDefault();search()}}><input autoFocus aria-label="Scan barcode or search"value={query}onChange={e=>setQuery(e.target.value)}placeholder="Scan barcode, SKU or product"/><button>Search</button></form><div className="productList">{rows.map(p=><button key={p.id}onClick={()=>add(p)}><div><b>{p.product_name}</b><small>{p.variant_name} · {p.sku}</small></div><span>KSh {p.selling_price}</span><small>{p.product_type==="SERVICE"?"Service":p.available+" available"}</small>{p.locations.map(l=><em key={l.shelf_code}>{l.zone} · {l.shelf_code} — {l.available}</em>)}</button>)}</div></section><form className="saleCart"onSubmit={complete}><h2>Current sale</h2>{!cart.length?<Empty>Scan or select an item.</Empty>:cart.map(x=><article key={x.p.id}><b>{x.p.product_name}</b><input type="number"min="1"value={x.q}onChange={e=>setCart(c=>c.map(y=>y.p.id===x.p.id?{...y,q:+e.target.value}:y))}/><span>KSh {+x.p.selling_price*x.q}</span></article>)}<label>Discount<input name="discount"type="number"defaultValue="0"/></label><label>Payment<select name="payment_method"><option>CASH</option><option>MPESA</option><option>BANK</option><option>OTHER</option></select></label><label>Payment reference<input name="payment_reference"/></label><button disabled={!cart.length}>Complete sale</button></form></div></>}
export function ProductsAdminPage(){const{data,error,load}=useLoad<Product[]>("/api/v1/store/products/");if(error)return <ErrorState message={error}retry={load}/>;if(!data)return <Loading/>;return <><span className="eyebrow">Catalog</span><h1>Products</h1>{!data.length?<Empty>Add products through the catalog API or Django back office.</Empty>:<div className="table">{data.map(p=><div key={p.id}><b>{p.product_name}</b><span>{p.name}</span><span>{p.sku}</span><span>KSh {p.selling_price}</span><span>{p.available===null?"Service":p.available+" available"}</span></div>)}</div>}</>}
export function ReceivePage(){const[products,setProducts]=useState<Product[]>([]);const[zones,setZones]=useState<Zone[]>([]);const[msg,setMsg]=useState("");const[error,setError]=useState("");useEffect(()=>{Promise.all([api<Product[]>("/api/v1/store/products/"),api<Zone[]>("/api/v1/locations/zones/")]).then(([p,z])=>{setProducts(p);setZones(z)}).catch(e=>setError(e.message))},[]);const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));try{await api("/api/v1/inventory/receive/",{method:"POST",body:JSON.stringify({variant_id:f.variant_id,unit_cost:f.unit_cost,selling_price:f.selling_price,reference:f.reference,supplier_name:f.supplier_name,placements:[{shelf_id:f.shelf_id,quantity:f.quantity}]})});setMsg("Stock received and placed successfully.")}catch(err){setError((err as Error).message)}};return <><span className="eyebrow">Purchasing & placement</span><h1>Receive Stock</h1>{msg&&<div className="success">{msg}</div>}{error&&<p className="formError">{error}</p>}<form className="formCard wide"onSubmit={submit}><label>Product variant<select name="variant_id"required>{products.map(p=><option value={p.id}key={p.id}>{p.product_name} — {p.name}</option>)}</select></label><label>Quantity<input name="quantity"type="number"step=".001"min=".001"required/></label><label>Buying price per unit<input name="unit_cost"type="number"step=".01"min="0"required/></label><label>Current/new selling price<input name="selling_price"type="number"step=".01"min="0"required/></label><label>Supplier<input name="supplier_name"/></label><label>Invoice/reference<input name="reference"required/></label><label>Physical shelf<select name="shelf_id"required>{zones.flatMap(z=>z.shelves.filter(s=>s.active).map(s=><option value={s.id}key={s.id}>{z.name} · {s.code} — {s.display_name}</option>))}</select></label><button>Receive and place stock</button></form></>}
export function StaffOrdersPage(){const{data,error,load}=useLoad<Order[]>("/api/v1/commerce/staff/orders/");if(error)return <ErrorState message={error}retry={load}/>;if(!data)return <Loading/>;const complete=async(o:Order)=>{await api("/api/v1/commerce/staff/orders/"+o.id+"/complete/",{method:"POST",body:JSON.stringify({payment_method:o.payment_method||"CASH",idempotency_key:"fulfill:"+o.number})});load()};return <><span className="eyebrow">Ecommerce fulfillment</span><h1>Orders</h1>{!data.length?<Empty>No online orders.</Empty>:<div className="list">{data.map(o=><article key={o.id}><div><b>{o.number}</b><small>{o.status} · {o.payment_status}</small>{o.items.map(i=><p key={i.id}>{i.product_name} × {i.quantity}{i.pick_locations?.map(a=><em key={a.shelf_id}> · Pick shelf #{a.shelf_id}: {a.quantity}</em>)}</p>)}</div><strong>KSh {o.total}</strong>{!["PAID","COMPLETED"].includes(o.status)&&<button onClick={()=>void complete(o)}>Confirm payment & consume reservation</button>}</article>)}</div>}</>}
export function AnalyticsPage(){return <><span className="eyebrow">Reporting</span><h1>Analytics</h1><DashboardPage/></>}

