import{Link,NavLink,Outlet}from"react-router-dom";import{ShoppingCart}from"lucide-react";import{ThemePicker}from"../components/ThemePicker";import{useAuth}from"../auth/AuthContext";export function PublicLayout(){const{user,logout}=useAuth();return <><header><Link className="brand"to="/">LinTech<span>Digital Point</span></Link><nav><NavLink to="/">Home</NavLink><NavLink to="/shop">Shop</NavLink><NavLink to="/cart"><ShoppingCart size={18}/> Cart</NavLink>{user&&!user.is_staff?<NavLink to="/account">Account</NavLink>:<NavLink to="/login">Login</NavLink>}</nav>{user&&<button className="linkButton"onClick={()=>void logout()}>Logout</button>}<ThemePicker/></header><Outlet/><footer>© LinTech Digital Point · Nairobi, Kenya</footer></>}

