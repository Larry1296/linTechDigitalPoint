import{expect,test}from"@playwright/test";test("public storefront and protected checkout",async({page})=>{const errors:string[]=[];page.on("console",m=>{if(m.type()==="error")errors.push(m.text())});await page.goto("/");await expect(page.getByRole("heading",{name:"Your trusted digital point."})).toBeVisible();await page.getByRole("link",{name:"Shop products"}).click();await expect(page.getByRole("heading",{name:"Shop"})).toBeVisible();await page.goto("/checkout");await expect(page).toHaveURL(/\/login\?next=%2Fcheckout/);await expect(page.getByRole("heading",{name:"Welcome back"})).toBeVisible();expect(errors).toEqual([])});test("staff login is separate and guarded",async({page})=>{await page.goto("/admin-app/dashboard");await expect(page).toHaveURL(/\/staff\/login/);await expect(page.getByRole("heading",{name:"Staff login"})).toBeVisible();await expect(page.getByRole("link",{name:"Create customer account"})).toHaveCount(0)});

