# Deployment

Use PostgreSQL only. Store environment values in a secret manager. Enable HTTPS, secure cookies, strict allowed hosts, database backups, log retention and monitoring. Run migrations before deployment. Serve Django with Gunicorn. Daraja needs external credentials and a verified public HTTPS callback.

