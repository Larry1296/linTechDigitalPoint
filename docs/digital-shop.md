# Digital Shop

A store owns extensible zones. Each zone defines a coordinate space; shelves persist x/y/width/height, depth and rotation. Shelves are unequal and not grid constrained. Permanent codes identify history independently of editable names. Contents are inventory aggregates, never manually typed descriptions. Empty zones show onboarding rather than fake geometry. Optional parent shelves represent compartments.

